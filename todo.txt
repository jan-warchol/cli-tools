change $bashsettings to $settings or sth else
use $settings in crontab

apparently it would make more sense to have currently-worked-on lilypond built in LILYPOND_GIT, so i need a few shortcuts for that.  It would be also good to have an indication of dirty tree if we're building that.

analyze git settings from indicidual repositories
