# to check which opetaring system is running a script,
# use this command
uname -s
# it returns
# "MINGW32_NT-6.1" when called from git bash in win7,
# "Linux" when called from bash in ubuntu 12.04

# or this environment variable
$OSTYPE
# it's value is
# "msys" when checked from git bash in win7,
# "linux-gnu" when checked from bash in ubuntu 12.04
