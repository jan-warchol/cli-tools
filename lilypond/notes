
chyba nie powinno być lily-builds, tylko powinno być obliczane z lilypond-build-dir

what make script needs to know:
- commit from which to build
- name of the directory to build to
- which make to use
- from which repository to call it (main or mirror)

pamiętać, że przy zmianie repozytorium trzeba odpalić configure

pamiętać, że trzeba odpalać autogena

pamiętać, że commity znajdujące się tylko na remote branches nie będą w sklonowanym repo


